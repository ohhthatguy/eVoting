import { useContext, useState } from "react"
import { AuthContext } from "../../context/auth"

const CitizenHomePage = () => {

    // const user = useContext(AuthContext)


  return (
    <div>
        this is citizen homepage

    </div>
  )
}

export default CitizenHomePage